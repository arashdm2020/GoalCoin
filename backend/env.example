# Database
DATABASE_URL="postgresql://user:password@localhost:5432/goalcoin?schema=public"

# Server
PORT=3001
FRONTEND_URL="http://localhost:3000"

# Admin Authentication
# Generate hash using: node -e "const bcrypt = require('bcrypt'); bcrypt.hash('your-password', 10).then(hash => console.log(hash));"
ADMIN_PASSWORD_HASH=""
ADMIN_USERNAME="admin"
